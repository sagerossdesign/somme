# One-Shot New Thread Prompt

Copy this into a new Codex thread and fill in details:

---
Use the `brand-site-generator` skill.

Build a complete premium static website from template in this repo (`index.html`, `styles.css`, `script.js`).

Brand brief:
- Brand name:
- Tone:
- Main background color:
- Accent color:
- Font(s):
- Menu items:
- Hero headline + CTA:
- Product section requirements:
- Story section panel copy:
- Subscription section copy + CTA:
- Footer copy:
- Asset filenames I uploaded:
- Scroll behavior preferences (desktop + mobile):

Requirements:
- Keep side margins consistent across all sections.
- Optimize scroll interactions for both mouse wheel and trackpad.
- Wire my uploaded assets by exact filename.
- Test locally first.

Output:
1) Implement all code changes
2) Summarize file changes
3) Provide local preview URL
---
