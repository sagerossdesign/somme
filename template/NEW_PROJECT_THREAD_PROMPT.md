# New Project Thread Prompt (Foundation-Based)

Copy this into a new Codex thread:

---
Use the `site-foundation-architect` skill.

Create a new project from foundation in this repo.

Project details:
- Project name:
- Project type preset (service-business / ecommerce-brand / portfolio-studio):
- Brand name:
- Brand colors:
- Typography:
- Section requirements:
- Animation requirements:
- Asset filenames:
- Desktop/mobile behavior preferences:

Execution requirements:
1. Scaffold using `foundation/scripts/new-site.sh`.
2. Build complete site in `sites/<project-name>/`.
3. Keep architecture (`index.html`, `styles.css`, `script.js`).
4. Test locally first.
5. Return file summary + preview instructions.
---
